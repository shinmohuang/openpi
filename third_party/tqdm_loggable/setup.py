from setuptools import setup

setup(
    name="tqdm-loggable",
    version="0.2.0",
    author="Michael Fogleman",
    author_email="",
    packages=["tqdm_loggable"],
    install_requires=["tqdm>=4.64"],
    description="tqdm progress bars that integrate with logging.",
)

